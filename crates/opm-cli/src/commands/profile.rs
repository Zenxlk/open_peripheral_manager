//! `pmctl profile` — not yet implemented.
//!
//! Will list, switch, and manage on-device profiles for peripherals that
//! expose the corresponding `opm-core` capability.

/// Runs the `profile` subcommand.
pub fn run() {
    println!("`pmctl profile` is not implemented yet.");
}
