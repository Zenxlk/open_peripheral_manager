# 2026-07-08 — Project kickoff

Set up the initial project skeleton: Cargo workspace, `opm-core` (empty
placeholder modules — traits intentionally left undesigned, see
`docs/architecture/driver-model.md`), `opm-cli` (`pmctl` binary with the
`list`/`info`/`rgb`/`profile` subcommands stubbed out), the `drivers/`
convention (no drivers yet), quality tooling (rustfmt, clippy lints,
editorconfig, CI), and the documentation structure itself.

No HID communication, protocol logic, or reverse engineering yet — that
starts once the AK820 is in hand.

Next: design the `Device`/`Driver`/capability traits in `opm-core`
(hands-on, as a Rust learning exercise) before touching any hardware.
