# Findings

Nothing yet — start logging here once captures begin.

Suggested format per entry:

```
## YYYY-MM-DD — short title

What was tested, what capture it came from (see `captures/`), what was
learned, what's still unknown.
```
